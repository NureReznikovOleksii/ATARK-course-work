# **SmartEnergy - Система моніторингу енергоспоживання**

**SmartEnergy** — це інтегрована апаратно-програмна система, створена для моніторингу, аналізу та оптимізації використання електроенергії. Проект дозволяє у реальному часі контролювати споживання енергії в різних приміщеннях, знижувати витрати та запобігати перевантаженню системи.

## **Особливості проекту**
- **Моніторинг енергоспоживання**:
  - Контроль потужності у реальному часі через датчики PZEM-004T.
  - Інтерактивний інтерфейс на LCD-екрані для відображення поточних даних.

- **Попередження про перевищення ліміту**:
  - Сповіщення про перевищення максимальної потужності через звукові сигнали.
  - Автоматичне відключення перевантажених приміщень для захисту системи.

- **Серверна інтеграція**:
  - REST API для збереження та обробки даних у базі даних MS SQL Server.
  - Динамічне оновлення максимальних лімітів потужності через сервер.

- **Гнучка архітектура**:
  - Масштабованість для додавання нових пристроїв або приміщень.
  - Підтримка роботи з хмарними сервісами.

## **Структура проекту**
1. **Апаратна частина**:
   - Плата ESP32.
   - Датчики потужності PZEM-004T.
   - LCD-екран для візуалізації.

2. **Програмна частина**:
   - **Сервер**:
     - Написаний на Node.js з використанням Express.
     - Інтеграція з MS SQL Server для збереження логів енергоспоживання, сповіщень та налаштувань.
   - **Клієнтська частина**:
     - ESP32 підключається до Wi-Fi та надсилає дані на сервер через REST API.
   - **Підсистеми**:
     - Система сповіщень про перевищення потужності.
     - Звукові та світлові сигнали при аварійних ситуаціях.

## **Основні функції**
- Вимірювання потужності для двох приміщень у реальному часі.
- Автоматичне оновлення максимальних лімітів потужності з бази даних.
- Збереження логів енергоспоживання на сервері.
- Звукові та візуальні сповіщення про перевищення потужності.

## **Як запустити проект**
### **Серверна частина**
1. Клонувати репозиторій:
   ```bash
   git clone https://github.com/your-repo-url.git
   cd SmartEnergyServer
   ```
2. Встановити залежності:
   ```bash
   npm install
   ```
3. Налаштувати базу даних:
   - Додати файл `.env` з такими параметрами:
     ```
     DB_DATABASE=SmartEnergyDB
     DB_USER=your_username
     DB_PASSWORD=your_password
     DB_SERVER=localhost
     DB_PORT=1433
     JWT_SECRET=your_secret_key
     ```
4. Запустити сервер:
   ```bash
   node app.js
   ```

### **Апаратна частина**
1. Завантажити скетч ESP32 з папки `arduino/SmartEnergy`.
2. Підключити ESP32 до Wi-Fi (SSID: `Wokwi-GUEST`).
3. Запустити систему та перевірити з'єднання з сервером.

## **Технології**
- **Node.js**: Серверна платформа для обробки REST API.
- **Express.js**: Фреймворк для створення маршрутизації.
- **MS SQL Server**: База даних для збереження налаштувань, логів та сповіщень.
- **ESP32**: Апаратна частина для збору даних та їх передачі.
- **PZEM-004T**: Датчики потужності.

## **Ліцензія**
Проект розповсюджується під ліцензією MIT. Ви можете вільно використовувати, змінювати та поширювати код з відповідними згадками.

---

## **Детальна демонстрація**
Деталі реалізації та демонстрація роботи представлені у відео:  
[**Демонстрація системи SmartEnergy**](https://youtu.be/nE3XVGzPIw8)

---

Дякуємо за вашу увагу! 🚀